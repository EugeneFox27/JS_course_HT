var x = [1, 180];
var y = [1, 180];
var count;
var sw;

for (count = 1; count <= 2; count++) {
    for (x = 1; x <= 50; x++) {
        document.write('.');
    }
    document.write('<br />')
    for (y = 1; y <= 2; y++) {
        document.write('\'' + '<br />');
    }
}